#!/usr/bin/env bash
# YumiOS-Klipper-V2 — Vanilla CustomPiOS config
# This file is populated at build time by the CI action which appends:
#   config/default → config/<type>/default → config/<type>/<sbc>
# Do NOT add config values here — use the config/ hierarchy instead.
